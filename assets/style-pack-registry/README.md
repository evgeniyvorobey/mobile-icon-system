# Style-Pack Registry Assets

`registry.json` is generated by:

```bash
python3 scripts/build_style_pack_registry.py
```

The file is a discovery index only. It must not be used to skip plugin
validation, Brand DNA conflict review, accessibility review, or Phase 5 user
confirmation.
